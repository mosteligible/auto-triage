[project]
name = "test-app"
version = "0.1.0"
description = "FastAPI failure generator instrumented with Logfire/OpenTelemetry."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.0",
    "logfire[fastapi]>=4.0.0",
    "python-dotenv>=1.2.2",
    "uvicorn[standard]>=0.35.0",
]
